const number = 65;
let copyOfNumber = number;
let binaryRepresentation = 0;
if (copyOfNumber >= 1) {
  const remainderWhenHalfed = copyOfNumber % 2;
  binaryRepresentation = binaryRepresentation * 10 + remainderWhenHalfed;
  copyOfNumber = (copyOfNumber - remainderWhenHalfed) / 2;
  console.log("remainder :", remainderWhenHalfed);
  console.log("new Number :", copyOfNumber);
  if (copyOfNumber >= 1) {
    const remainderWhenHalfed = copyOfNumber % 2;
    binaryRepresentation = binaryRepresentation * 10 + remainderWhenHalfed;
    copyOfNumber = (copyOfNumber - remainderWhenHalfed) / 2;
    console.log("remainder :", remainderWhenHalfed);
    console.log("new Number :", copyOfNumber);
    if (copyOfNumber >= 1) {
      const remainderWhenHalfed = copyOfNumber % 2;
      binaryRepresentation = binaryRepresentation * 10 + remainderWhenHalfed;
      copyOfNumber = (copyOfNumber - remainderWhenHalfed) / 2;
      console.log("remainder :", remainderWhenHalfed);
      console.log("new Number :", copyOfNumber);
      if (copyOfNumber >= 1) {
        const remainderWhenHalfed = copyOfNumber % 2;
        binaryRepresentation = binaryRepresentation * 10 + remainderWhenHalfed;
        copyOfNumber = (copyOfNumber - remainderWhenHalfed) / 2;
        console.log("remainder :", remainderWhenHalfed);
        console.log("new Number :", copyOfNumber);
        if (copyOfNumber >= 1) {
          const remainderWhenHalfed = copyOfNumber % 2;
          binaryRepresentation = binaryRepresentation * 10 + remainderWhenHalfed;
          copyOfNumber = (copyOfNumber - remainderWhenHalfed) / 2;
          console.log("remainder :", remainderWhenHalfed);
          console.log("new Number :", copyOfNumber);
          if (copyOfNumber >= 1) {
            const remainderWhenHalfed = copyOfNumber % 2;
            binaryRepresentation = binaryRepresentation * 10 + remainderWhenHalfed;
            copyOfNumber = (copyOfNumber - remainderWhenHalfed) / 2;
            console.log("remainder :", remainderWhenHalfed);
            console.log("new Number :", copyOfNumber);
            if (copyOfNumber >= 1) {
              const remainderWhenHalfed = copyOfNumber % 2;
              binaryRepresentation = binaryRepresentation * 10 + remainderWhenHalfed;
              copyOfNumber = (copyOfNumber - remainderWhenHalfed) / 2;
              console.log("remainder :", remainderWhenHalfed);
              console.log("new Number :", copyOfNumber);
              if (copyOfNumber >= 1) {
                const remainderWhenHalfed = copyOfNumber % 2;
                binaryRepresentation = binaryRepresentation * 10 + remainderWhenHalfed;
                copyOfNumber = (copyOfNumber - remainderWhenHalfed) / 2;
                console.log("remainder :", remainderWhenHalfed);
                console.log("new Number :", copyOfNumber);
                if (copyOfNumber >= 1) {
                  const remainderWhenHalfed = copyOfNumber % 2;
                  binaryRepresentation = binaryRepresentation * 10 + remainderWhenHalfed;
                  copyOfNumber = (copyOfNumber - remainderWhenHalfed) / 2;
                  console.log("remainder :", remainderWhenHalfed);
                  console.log("new Number :", copyOfNumber);
                  if (copyOfNumber >= 1) {
                    const remainderWhenHalfed = copyOfNumber % 2;
                    binaryRepresentation = binaryRepresentation * 10 + remainderWhenHalfed;
                    copyOfNumber = (copyOfNumber - remainderWhenHalfed) / 2;
                    console.log("remainder :", remainderWhenHalfed);
                    console.log("new Number :", copyOfNumber);
                    if (copyOfNumber >= 1) {
                      const remainderWhenHalfed = copyOfNumber % 2;
                      binaryRepresentation = binaryRepresentation * 10 + remainderWhenHalfed;
                      copyOfNumber = (copyOfNumber - remainderWhenHalfed) / 2;
                      console.log("remainder :", remainderWhenHalfed);
                      console.log("new Number :", copyOfNumber);
                      if (copyOfNumber >= 1) {
                        const remainderWhenHalfed = copyOfNumber % 2;
                        binaryRepresentation = binaryRepresentation * 10 + remainderWhenHalfed;
                        copyOfNumber = (copyOfNumber - remainderWhenHalfed) / 2;
                        console.log("remainder :", remainderWhenHalfed);
                        console.log("new Number :", copyOfNumber);
                        if (copyOfNumber >= 1) {
                          const remainderWhenHalfed = copyOfNumber % 2;
                          binaryRepresentation = binaryRepresentation * 10 + remainderWhenHalfed;
                          copyOfNumber = (copyOfNumber - remainderWhenHalfed) / 2;
                          console.log("remainder :", remainderWhenHalfed);
                          console.log("new Number :", copyOfNumber);
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
console.log("The binary representation of", number, "is", binaryRepresentation);
